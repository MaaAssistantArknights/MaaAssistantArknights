name: smoke-testing

on:
  push:
    paths:
      - "3rdparty/include/**"
      - "include/**"
      - "src/**"
      - "cmake/**"
      - "CMakeLists.txt"
      - "MAA.sln"
      - "resource/**"
      - "MaaDeps/**"
      - "!**/*.md"
  pull_request:
    paths:
      - "3rdparty/include/**"
      - "include/**"
      - "src/**"
      - "cmake/**"
      - "CMakeLists.txt"
      - "MAA.sln"
      - "resource/**"
      - "MaaDeps/**"
      - "!**/*.md"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    # Prevent duplicate runs on organization branches with PRs
    if: github.event_name != 'pull_request' ||
      github.event.pull_request.head.repo.full_name !=
      github.event.pull_request.base.repo.full_name
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          show-progress: false
          submodules: recursive

      - name: Cache .nuke/temp, ~/.nuget/packages
        id: cache-nuget
        uses: actions/cache@v4
        with:
          path: |
            .nuke/temp
            ~/.nuget/packages
          key: ${{ runner.os }}-x64-${{ hashFiles('**/global.json', '**/*.csproj') }}${{runner.arch}}

      - name: Restore dependencies
        if: steps.cache-nuget.outputs.cache-hit != 'true'
        run: dotnet restore

      - name: Cache MaaDeps
        id: cache-maadeps
        uses: actions/cache@v4
        with:
          path: |
            ./MaaDeps
          key: ${{ runner.os }}-x64-maadeps-${{ hashFiles('./maadeps-download.py') }}

      - name: Bootstrap MaaDeps
        if: steps.cache-maadeps.outputs.cache-hit != 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          python3 maadeps-download.py x64-windows

      - name: Add msbuild to PATH
        uses: microsoft/setup-msbuild@v2

      # - name: Restore incremental build
      #   uses: actions/cache/restore@v4
      #   with:
      #     key: smoke-testing-incremental
      #     path: |
      #       ./src/MaaCore/x64/Debug
      #       ./src/Cpp/x64/Debug

      - name: Build MaaSample
        run: |
          MSBUILD src/Cpp/MaaSample.slnf -t:restore,build -p:Configuration="Debug" -p:Platform="x64" -m

      # - name: Save incremental build
      #   uses: actions/cache/save@v4
      #   with:
      #     key: smoke-testing-incremental
      #     path: |
      #       ./src/MaaCore/x64/Debug
      #       ./src/Cpp/x64/Debug

      - name: Save build artifact to cache
        uses: actions/cache/save@v4
        with:
          key: DELETE_KEY-smoke-testing-artifact
          path: |
            ./x64/Debug

  test:
    # Prevent duplicate runs on organization branches with PRs
    if: github.event_name != 'pull_request' ||
      github.event.pull_request.head.repo.full_name !=
      github.event.pull_request.base.repo.full_name

    needs: build
    runs-on: windows-latest
    strategy:
      matrix:
        client: [Official, YostarJP, YostarEN, YostarKR, txwy]

    steps:
      - name: Restore build artifact from cache
        id: cache-smoke-testing
        uses: actions/cache/restore@v4
        with:
          key: DELETE_KEY-smoke-testing-artifact-${{ github.ref }}
          path: |
            ./x64/Debug

      - name: Run tests - ${{ matrix.client }}
        continue-on-error: true
        run: |
          ./x64/Debug/Sample.exe ${{ matrix.client }}

      - name: Upload logs
        uses: actions/upload-artifact@v4
        with:
          name: log-${{ matrix.client }}
          path: ./x64/Debug/debug

      - name: Delete cache
        if: ${{ matrix.client == 'Official' }}
        run: |
          echo "Deleting the cache..."
          gh cache delete DELETE_KEY-smoke-testing-artifact-${{ github.ref }} --confirm
