<UserControl
    x:Class="MaaWpfGui.Views.UserControl.Settings.TimerSettingsUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:c="clr-namespace:CalcBinding;assembly=CalcBinding"
    xmlns:controls="clr-namespace:MaaWpfGui.Styles.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dd="urn:gong-wpf-dragdrop"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:helper="clr-namespace:MaaWpfGui.Helper"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:s="https://github.com/canton7/Stylet"
    xmlns:settings_vms="clr-namespace:MaaWpfGui.ViewModels.UserControl.Settings"
    d:Background="White"
    d:DataContext="{d:DesignInstance {x:Type settings_vms:TimerSettingsUserControlModel}}"
    d:DesignHeight="450"
    s:View.ActionTarget="{Binding}"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <controls:GlobalConfigTip Grid.Row="0" />
        <StackPanel
            Grid.Row="1"
            HorizontalAlignment="Center"
            Orientation="Horizontal">
            <StackPanel Margin="5,0" Orientation="Horizontal">
                <CheckBox
                    Height="30"
                    HorizontalAlignment="Center"
                    VerticalContentAlignment="Center"
                    Content="{DynamicResource ForceScheduledStart}"
                    IsChecked="{Binding ForceScheduledStart}" />
                <controls:TooltipBlock TooltipText="{DynamicResource ForceScheduledStartTip}" />
            </StackPanel>

            <StackPanel Margin="5,0" Orientation="Horizontal">
                <CheckBox
                    Height="30"
                    HorizontalAlignment="Center"
                    VerticalContentAlignment="Center"
                    Content="{DynamicResource TimerCustomConfig}"
                    IsChecked="{Binding CustomConfig}" />
                <controls:TooltipBlock TooltipText="{DynamicResource TimerCustomConfigTip}" />
            </StackPanel>
        </StackPanel>

        <StackPanel
            Grid.Row="2"
            HorizontalAlignment="Center"
            Orientation="Horizontal">
            <CheckBox
                Height="30"
                Margin="5,0"
                HorizontalAlignment="Center"
                VerticalContentAlignment="Center"
                Content="{DynamicResource ShowWindowBeforeForceScheduledStart}"
                IsChecked="{Binding ShowWindowBeforeForceScheduledStart}"
                Visibility="{c:Binding ForceScheduledStart}" />
        </StackPanel>

        <Grid Grid.Row="3">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Row="0" Grid.Column="0">
                <StackPanel
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal">
                    <CheckBox
                        Margin="10"
                        Content="{Binding TimerList[0].Name}"
                        IsChecked="{Binding TimerList[0].IsEnabled}"
                        MouseRightButtonUp="{s:Action ToggleCheckBoxNullOnRightClick,
                                                      Target={x:Type helper:CheckBoxHelper}}"
                        ToolTip="{DynamicResource CheckBoxesNotSavedAsNull}" />
                    <StackPanel Orientation="Horizontal">
                        <hc:NumericUpDown
                            Width="55"
                            Margin="10"
                            d:Value="0"
                            InputMethod.IsInputMethodEnabled="False"
                            IsEnabled="{c:Binding 'TimerList[0].IsEnabled ?? &quot;True&quot;'}"
                            Maximum="23"
                            Minimum="0"
                            ValueFormat="00"
                            Value="{Binding TimerList[0].Hour}" />
                        <controls:TextBlock Text=":" />
                        <hc:NumericUpDown
                            Width="55"
                            Margin="10"
                            d:Value="0"
                            InputMethod.IsInputMethodEnabled="False"
                            IsEnabled="{c:Binding 'TimerList[0].IsEnabled ?? &quot;True&quot;'}"
                            Maximum="59"
                            Minimum="0"
                            ValueFormat="00"
                            Value="{Binding TimerList[0].Minute}" />
                    </StackPanel>
                </StackPanel>
                <ComboBox
                    Width="150"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Focusable="False"
                    IsEnabled="{Binding Idle, Source={x:Static helper:Instances.SettingsViewModel}}"
                    ItemsSource="{Binding ConfigurationList, Source={x:Static helper:Instances.SettingsViewModel}}"
                    SelectedValue="{Binding TimerList[0].Config}"
                    SelectedValuePath="Value"
                    ToolTip="{Binding TimerList[0].Config}"
                    Visibility="{c:Binding CustomConfig}" />
            </StackPanel>

            <StackPanel Grid.Row="1" Grid.Column="0">
                <StackPanel
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal">
                    <CheckBox
                        Margin="10"
                        Content="{Binding TimerList[1].Name}"
                        IsChecked="{Binding TimerList[1].IsEnabled}"
                        MouseRightButtonUp="{s:Action ToggleCheckBoxNullOnRightClick,
                                                      Target={x:Type helper:CheckBoxHelper}}"
                        ToolTip="{DynamicResource CheckBoxesNotSavedAsNull}" />
                    <StackPanel Orientation="Horizontal">
                        <hc:NumericUpDown
                            Width="55"
                            Margin="10"
                            d:Value="3"
                            InputMethod.IsInputMethodEnabled="False"
                            IsEnabled="{c:Binding 'TimerList[1].IsEnabled ?? &quot;True&quot;'}"
                            Maximum="23"
                            Minimum="0"
                            ValueFormat="00"
                            Value="{Binding TimerList[1].Hour}" />
                        <controls:TextBlock Text=":" />
                        <hc:NumericUpDown
                            Width="55"
                            Margin="10"
                            d:Value="5"
                            InputMethod.IsInputMethodEnabled="False"
                            IsEnabled="{c:Binding 'TimerList[1].IsEnabled ?? &quot;True&quot;'}"
                            Maximum="59"
                            Minimum="0"
                            ValueFormat="00"
                            Value="{Binding TimerList[1].Minute}" />
                    </StackPanel>
                </StackPanel>
                <ComboBox
                    Width="150"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Focusable="False"
                    IsEnabled="{Binding Idle, Source={x:Static helper:Instances.SettingsViewModel}}"
                    ItemsSource="{Binding ConfigurationList, Source={x:Static helper:Instances.SettingsViewModel}}"
                    SelectedValue="{Binding TimerList[1].Config}"
                    SelectedValuePath="Value"
                    ToolTip="{Binding TimerList[1].Config}"
                    Visibility="{c:Binding CustomConfig}" />
            </StackPanel>

            <StackPanel Grid.Row="2" Grid.Column="0">
                <StackPanel
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal">
                    <CheckBox
                        Margin="10"
                        Content="{Binding TimerList[2].Name}"
                        IsChecked="{Binding TimerList[2].IsEnabled}"
                        MouseRightButtonUp="{s:Action ToggleCheckBoxNullOnRightClick,
                                                      Target={x:Type helper:CheckBoxHelper}}"
                        ToolTip="{DynamicResource CheckBoxesNotSavedAsNull}" />
                    <StackPanel Orientation="Horizontal">
                        <hc:NumericUpDown
                            Width="55"
                            Margin="10"
                            d:Value="6"
                            InputMethod.IsInputMethodEnabled="False"
                            IsEnabled="{c:Binding 'TimerList[2].IsEnabled ?? &quot;True&quot;'}"
                            Maximum="23"
                            Minimum="0"
                            ValueFormat="00"
                            Value="{Binding TimerList[2].Hour}" />
                        <controls:TextBlock Text=":" />
                        <hc:NumericUpDown
                            Width="55"
                            Margin="10"
                            d:Value="10"
                            InputMethod.IsInputMethodEnabled="False"
                            IsEnabled="{c:Binding 'TimerList[2].IsEnabled ?? &quot;True&quot;'}"
                            Maximum="59"
                            Minimum="0"
                            ValueFormat="00"
                            Value="{Binding TimerList[2].Minute}" />
                    </StackPanel>
                </StackPanel>
                <ComboBox
                    Width="150"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Focusable="False"
                    IsEnabled="{Binding Idle, Source={x:Static helper:Instances.SettingsViewModel}}"
                    ItemsSource="{Binding ConfigurationList, Source={x:Static helper:Instances.SettingsViewModel}}"
                    SelectedValue="{Binding TimerList[2].Config}"
                    SelectedValuePath="Value"
                    ToolTip="{Binding TimerList[2].Config}"
                    Visibility="{c:Binding CustomConfig}" />
            </StackPanel>

            <StackPanel Grid.Row="3" Grid.Column="0">
                <StackPanel
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal">
                    <CheckBox
                        Margin="10"
                        Content="{Binding TimerList[3].Name}"
                        IsChecked="{Binding TimerList[3].IsEnabled}"
                        MouseRightButtonUp="{s:Action ToggleCheckBoxNullOnRightClick,
                                                      Target={x:Type helper:CheckBoxHelper}}"
                        ToolTip="{DynamicResource CheckBoxesNotSavedAsNull}" />
                    <StackPanel Orientation="Horizontal">
                        <hc:NumericUpDown
                            Width="55"
                            Margin="10"
                            d:Value="9"
                            InputMethod.IsInputMethodEnabled="False"
                            IsEnabled="{c:Binding 'TimerList[3].IsEnabled ?? &quot;True&quot;'}"
                            Maximum="23"
                            Minimum="0"
                            ValueFormat="00"
                            Value="{Binding TimerList[3].Hour}" />
                        <controls:TextBlock Text=":" />
                        <hc:NumericUpDown
                            Width="55"
                            Margin="10"
                            d:Value="15"
                            InputMethod.IsInputMethodEnabled="False"
                            IsEnabled="{c:Binding 'TimerList[3].IsEnabled ?? &quot;True&quot;'}"
                            Maximum="59"
                            Minimum="0"
                            ValueFormat="00"
                            Value="{Binding TimerList[3].Minute}" />
                    </StackPanel>
                </StackPanel>
                <ComboBox
                    Width="150"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Focusable="False"
                    IsEnabled="{Binding Idle, Source={x:Static helper:Instances.SettingsViewModel}}"
                    ItemsSource="{Binding ConfigurationList, Source={x:Static helper:Instances.SettingsViewModel}}"
                    SelectedValue="{Binding TimerList[3].Config}"
                    SelectedValuePath="Value"
                    ToolTip="{Binding TimerList[3].Config}"
                    Visibility="{c:Binding CustomConfig}" />
            </StackPanel>

            <StackPanel Grid.Row="0" Grid.Column="1">
                <StackPanel
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal">
                    <CheckBox
                        Margin="10"
                        Content="{Binding TimerList[4].Name}"
                        IsChecked="{Binding TimerList[4].IsEnabled}"
                        MouseRightButtonUp="{s:Action ToggleCheckBoxNullOnRightClick,
                                                      Target={x:Type helper:CheckBoxHelper}}"
                        ToolTip="{DynamicResource CheckBoxesNotSavedAsNull}" />
                    <StackPanel Orientation="Horizontal">
                        <hc:NumericUpDown
                            Width="55"
                            Margin="10"
                            d:Value="12"
                            InputMethod.IsInputMethodEnabled="False"
                            IsEnabled="{c:Binding 'TimerList[4].IsEnabled ?? &quot;True&quot;'}"
                            Maximum="23"
                            Minimum="0"
                            ValueFormat="00"
                            Value="{Binding TimerList[4].Hour}" />
                        <controls:TextBlock Text=":" />
                        <hc:NumericUpDown
                            Width="55"
                            Margin="10"
                            d:Value="20"
                            InputMethod.IsInputMethodEnabled="False"
                            IsEnabled="{c:Binding 'TimerList[4].IsEnabled ?? &quot;True&quot;'}"
                            Maximum="59"
                            Minimum="0"
                            ValueFormat="00"
                            Value="{Binding TimerList[4].Minute}" />
                    </StackPanel>
                </StackPanel>
                <ComboBox
                    Width="150"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Focusable="False"
                    IsEnabled="{Binding Idle, Source={x:Static helper:Instances.SettingsViewModel}}"
                    ItemsSource="{Binding ConfigurationList, Source={x:Static helper:Instances.SettingsViewModel}}"
                    SelectedValue="{Binding TimerList[4].Config}"
                    SelectedValuePath="Value"
                    ToolTip="{Binding TimerList[4].Config}"
                    Visibility="{c:Binding CustomConfig}" />
            </StackPanel>

            <StackPanel Grid.Row="1" Grid.Column="1">
                <StackPanel
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal">
                    <CheckBox
                        Margin="10"
                        Content="{Binding TimerList[5].Name}"
                        IsChecked="{Binding TimerList[5].IsEnabled}"
                        MouseRightButtonUp="{s:Action ToggleCheckBoxNullOnRightClick,
                                                      Target={x:Type helper:CheckBoxHelper}}"
                        ToolTip="{DynamicResource CheckBoxesNotSavedAsNull}" />
                    <StackPanel Orientation="Horizontal">
                        <hc:NumericUpDown
                            Width="55"
                            Margin="10"
                            d:Value="15"
                            InputMethod.IsInputMethodEnabled="False"
                            IsEnabled="{c:Binding 'TimerList[5].IsEnabled ?? &quot;True&quot;'}"
                            Maximum="23"
                            Minimum="0"
                            ValueFormat="00"
                            Value="{Binding TimerList[5].Hour}" />
                        <controls:TextBlock Text=":" />
                        <hc:NumericUpDown
                            Width="55"
                            Margin="10"
                            d:Value="30"
                            InputMethod.IsInputMethodEnabled="False"
                            IsEnabled="{c:Binding 'TimerList[5].IsEnabled ?? &quot;True&quot;'}"
                            Maximum="59"
                            Minimum="0"
                            ValueFormat="00"
                            Value="{Binding TimerList[5].Minute}" />
                    </StackPanel>
                </StackPanel>
                <ComboBox
                    Width="150"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Focusable="False"
                    IsEnabled="{Binding Idle, Source={x:Static helper:Instances.SettingsViewModel}}"
                    ItemsSource="{Binding ConfigurationList, Source={x:Static helper:Instances.SettingsViewModel}}"
                    SelectedValue="{Binding TimerList[5].Config}"
                    SelectedValuePath="Value"
                    ToolTip="{Binding TimerList[5].Config}"
                    Visibility="{c:Binding CustomConfig}" />
            </StackPanel>

            <StackPanel Grid.Row="2" Grid.Column="1">
                <StackPanel
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal">
                    <CheckBox
                        Margin="10"
                        Content="{Binding TimerList[6].Name}"
                        IsChecked="{Binding TimerList[6].IsEnabled}"
                        MouseRightButtonUp="{s:Action ToggleCheckBoxNullOnRightClick,
                                                      Target={x:Type helper:CheckBoxHelper}}"
                        ToolTip="{DynamicResource CheckBoxesNotSavedAsNull}" />
                    <StackPanel Orientation="Horizontal">
                        <hc:NumericUpDown
                            Width="55"
                            Margin="10"
                            d:Value="18"
                            InputMethod.IsInputMethodEnabled="False"
                            IsEnabled="{c:Binding 'TimerList[6].IsEnabled ?? &quot;True&quot;'}"
                            Maximum="23"
                            Minimum="0"
                            ValueFormat="00"
                            Value="{Binding TimerList[6].Hour}" />
                        <controls:TextBlock Text=":" />
                        <hc:NumericUpDown
                            Width="55"
                            Margin="10"
                            d:Value="40"
                            InputMethod.IsInputMethodEnabled="False"
                            IsEnabled="{c:Binding 'TimerList[6].IsEnabled ?? &quot;True&quot;'}"
                            Maximum="59"
                            Minimum="0"
                            ValueFormat="00"
                            Value="{Binding TimerList[6].Minute}" />
                    </StackPanel>
                </StackPanel>
                <ComboBox
                    Width="150"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Focusable="False"
                    IsEnabled="{Binding Idle, Source={x:Static helper:Instances.SettingsViewModel}}"
                    ItemsSource="{Binding ConfigurationList, Source={x:Static helper:Instances.SettingsViewModel}}"
                    SelectedValue="{Binding TimerList[6].Config}"
                    SelectedValuePath="Value"
                    ToolTip="{Binding TimerList[6].Config}"
                    Visibility="{c:Binding CustomConfig}" />
            </StackPanel>

            <StackPanel Grid.Row="3" Grid.Column="1">
                <StackPanel
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal">
                    <CheckBox
                        Margin="10"
                        Content="{Binding TimerList[7].Name}"
                        IsChecked="{Binding TimerList[7].IsEnabled}"
                        MouseRightButtonUp="{s:Action ToggleCheckBoxNullOnRightClick,
                                                      Target={x:Type helper:CheckBoxHelper}}"
                        ToolTip="{DynamicResource CheckBoxesNotSavedAsNull}" />
                    <StackPanel Orientation="Horizontal">
                        <hc:NumericUpDown
                            Width="55"
                            Margin="10"
                            d:Value="21"
                            InputMethod.IsInputMethodEnabled="False"
                            IsEnabled="{c:Binding 'TimerList[7].IsEnabled ?? &quot;True&quot;'}"
                            Maximum="23"
                            Minimum="0"
                            ValueFormat="00"
                            Value="{Binding TimerList[7].Hour}" />
                        <controls:TextBlock Text=":" />
                        <hc:NumericUpDown
                            Width="55"
                            Margin="10"
                            d:Value="50"
                            InputMethod.IsInputMethodEnabled="False"
                            IsEnabled="{c:Binding 'TimerList[7].IsEnabled ?? &quot;True&quot;'}"
                            Maximum="59"
                            Minimum="0"
                            ValueFormat="00"
                            Value="{Binding TimerList[7].Minute}" />
                    </StackPanel>
                </StackPanel>
                <ComboBox
                    Width="150"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Focusable="False"
                    IsEnabled="{Binding Idle, Source={x:Static helper:Instances.SettingsViewModel}}"
                    ItemsSource="{Binding ConfigurationList, Source={x:Static helper:Instances.SettingsViewModel}}"
                    SelectedValue="{Binding TimerList[7].Config}"
                    SelectedValuePath="Value"
                    ToolTip="{Binding TimerList[7].Config}"
                    Visibility="{c:Binding CustomConfig}" />
            </StackPanel>

        </Grid>
    </Grid>
</UserControl>
