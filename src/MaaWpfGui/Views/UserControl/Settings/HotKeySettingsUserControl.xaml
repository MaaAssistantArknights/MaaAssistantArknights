<UserControl
    x:Class="MaaWpfGui.Views.UserControl.Settings.HotKeySettingsUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:c="clr-namespace:CalcBinding;assembly=CalcBinding"
    xmlns:controls="clr-namespace:MaaWpfGui.Styles.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helper="clr-namespace:MaaWpfGui.Helper"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:settings="clr-namespace:MaaWpfGui.Views.UserControl.Settings"
    xmlns:ui_vms="clr-namespace:MaaWpfGui.ViewModels.UI"
    d:DataContext="{d:DesignInstance {x:Type ui_vms:SettingsViewModel}}"
    mc:Ignorable="d">
    <StackPanel
        Margin="10"
        HorizontalAlignment="Center"
        VerticalAlignment="Center">
        <controls:GlobalConfigTip Grid.Row="0" />
        <StackPanel
            HorizontalAlignment="Right"
            VerticalAlignment="Center"
            Orientation="Horizontal">
            <controls:TextBlock
                Margin="10"
                Block.TextAlignment="Center"
                Text="{DynamicResource HotKeyShowGui}" />
            <settings:HotKeyEditorUserControl
                Margin="10,10,0,10"
                HorizontalAlignment="Right"
                HotKey="{Binding HotKeyShowGui}" />
            <controls:TooltipBlock TooltipText="{DynamicResource HotKeyChangingTip}" />
        </StackPanel>
        <StackPanel
            HorizontalAlignment="Right"
            VerticalAlignment="Center"
            Orientation="Horizontal">
            <controls:TextBlock
                Margin="10"
                Block.TextAlignment="Center"
                Text="{DynamicResource HotKeyLinkStart}" />
            <settings:HotKeyEditorUserControl
                Margin="10,10,0,10"
                HorizontalAlignment="Right"
                HotKey="{Binding HotKeyLinkStart}" />
            <controls:TooltipBlock TooltipText="{DynamicResource HotKeyChangingTip}" />
        </StackPanel>
    </StackPanel>
</UserControl>
