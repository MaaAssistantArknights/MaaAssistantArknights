<UserControl
    x:Class="MaaWpfGui.Views.UserControl.Settings.StartSettingsUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:c="clr-namespace:CalcBinding;assembly=CalcBinding"
    xmlns:controls="clr-namespace:MaaWpfGui.Styles.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dd="urn:gong-wpf-dragdrop"
    xmlns:enums="clr-namespace:MaaWpfGui.Constants.Enums"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:helper="clr-namespace:MaaWpfGui.Helper"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:s="https://github.com/canton7/Stylet"
    xmlns:settings_vms="clr-namespace:MaaWpfGui.ViewModels.UserControl.Settings"
    xmlns:ui_vms="clr-namespace:MaaWpfGui.ViewModels.UI"
    d:Background="White"
    d:DataContext="{d:DesignInstance {x:Type settings_vms:StartSettingsUserControlModel}}"
    d:DesignHeight="300"
    d:DesignWidth="500"
    s:View.ActionTarget="{Binding}"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="250" />
        </Grid.ColumnDefinitions>
        <StackPanel
            Grid.Column="0"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Orientation="Vertical">
            <controls:GlobalConfigTip />
            <StackPanel Margin="10" Orientation="Horizontal">
                <CheckBox
                    HorizontalAlignment="Left"
                    Block.TextAlignment="Center"
                    Content="{DynamicResource LaunchOnSystemStartup}"
                    IsChecked="{Binding StartSelf}" />
                <controls:TooltipBlock TooltipText="{DynamicResource LaunchOnSystemStartupWarning}" />
            </StackPanel>
            <CheckBox
                Margin="10"
                HorizontalAlignment="Left"
                Block.TextAlignment="Center"
                Content="{DynamicResource MinimizeAfterLaunch}"
                IsChecked="{Binding MinimizeDirectly}" />
            <hc:Divider />
            <CheckBox
                Margin="10"
                HorizontalAlignment="Left"
                Block.TextAlignment="Center"
                Content="{DynamicResource RunTaskAfterLaunch}"
                IsChecked="{Binding RunDirectly}" />
            <CheckBox
                Margin="10"
                HorizontalAlignment="Left"
                Block.TextAlignment="Center"
                Content="{DynamicResource OpenEmulatorAfterLaunch}"
                IsChecked="{Binding OpenEmulatorAfterLaunch}"
                Visibility="{c:Binding 'ConnectConfig != enums:ConnectConfig.PC', Source={x:Static settings_vms:ConnectSettingsUserControlModel.Instance}}" />
            <CheckBox
                Margin="10"
                HorizontalAlignment="Left"
                Block.TextAlignment="Center"
                IsChecked="{Binding OpenEmulatorAfterLaunch}"
                Visibility="{c:Binding 'ConnectConfig == enums:ConnectConfig.PC', Source={x:Static settings_vms:ConnectSettingsUserControlModel.Instance}}">
                <controls:TextBlock
                    MaxWidth="210"
                    Text="{DynamicResource OpenPcClientAfterLaunch}"
                    TextWrapping="Wrap" />
            </CheckBox>
            <StackPanel Margin="10" Orientation="Horizontal">
                <CheckBox MaxWidth="210" IsChecked="{Binding SkipStartupAutoRunAfterUpdate}">
                    <controls:TextBlock Text="{DynamicResource SkipStartupAutoRunAfterUpdate}" TextWrapping="Wrap" />
                </CheckBox>
                <controls:TooltipBlock Grid.Column="1" TooltipText="{DynamicResource SkipStartupAutoRunAfterUpdateTip}" />
            </StackPanel>
            <StackPanel
                Margin="10"
                Orientation="Horizontal"
                Visibility="{c:Binding 'ConnectConfig != enums:ConnectConfig.PC', Source={x:Static settings_vms:ConnectSettingsUserControlModel.Instance}}">
                <CheckBox MaxWidth="210" IsChecked="{Binding RetryOnDisconnected, Source={x:Static ui_vms:SettingsViewModel.ConnectSettings}}">
                    <controls:TextBlock Text="{DynamicResource RetryOnDisconnected}" TextWrapping="Wrap" />
                </CheckBox>
                <controls:TooltipBlock Grid.Column="1" TooltipText="{DynamicResource RetryOnDisconnectedTip}" />
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Column="1" VerticalAlignment="Center">
            <StackPanel Margin="10">
                <StackPanel
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal">
                    <controls:TextBlock
                        Margin="0,5"
                        VerticalAlignment="Center"
                        Block.TextAlignment="Center"
                        Text="{DynamicResource EmulatorPath}"
                        Visibility="{c:Binding 'ConnectConfig != enums:ConnectConfig.PC', Source={x:Static settings_vms:ConnectSettingsUserControlModel.Instance}}" />
                    <controls:TextBlock
                        Margin="0,5"
                        VerticalAlignment="Center"
                        Block.TextAlignment="Center"
                        Text="{DynamicResource PcClientPath}"
                        Visibility="{c:Binding 'ConnectConfig == enums:ConnectConfig.PC', Source={x:Static settings_vms:ConnectSettingsUserControlModel.Instance}}" />
                    <controls:TooltipBlock
                        TooltipText="{DynamicResource SelectShortcutTip}"
                        Visibility="{c:Binding 'ConnectConfig != enums:ConnectConfig.PC', Source={x:Static settings_vms:ConnectSettingsUserControlModel.Instance}}" />
                    <controls:TooltipBlock
                        TooltipText="{DynamicResource PcClientPathTip}"
                        Visibility="{c:Binding 'ConnectConfig == enums:ConnectConfig.PC', Source={x:Static settings_vms:ConnectSettingsUserControlModel.Instance}}" />
                    <Button
                        Margin="10,0,0,0"
                        hc:BorderElement.CornerRadius="4,0,0,4"
                        Command="{s:Action SelectEmulatorExec}"
                        Content="{DynamicResource Select}" />
                    <Button
                        hc:BorderElement.CornerRadius="0,4,4,0"
                        BorderThickness="0,1,1,1"
                        Command="{s:Action TestEmulatorExec}"
                        Content="{DynamicResource Test}" />
                </StackPanel>
                <TextBox Text="{Binding EmulatorPath}" />
            </StackPanel>
            <StackPanel
                Margin="10"
                Visibility="{c:Binding 'ConnectConfig != enums:ConnectConfig.PC', Source={x:Static settings_vms:ConnectSettingsUserControlModel.Instance}}">
                <StackPanel
                    Margin="0,0,0,5"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal">
                    <controls:TextBlock Block.TextAlignment="Center" Text="{DynamicResource AdditionCommand}" />
                    <controls:TooltipBlock TooltipText="{DynamicResource AdditionCommandTip}" />
                </StackPanel>
                <TextBox IsEnabled="{Binding EmulatorPath.Length}" Text="{Binding EmulatorAddCommand}" />
            </StackPanel>
            <StackPanel
                Margin="10"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Orientation="Horizontal">
                <controls:TextBlock
                    Margin="3"
                    Block.TextAlignment="Center"
                    Text="{DynamicResource WaitForEmulator}"
                    Visibility="{c:Binding 'ConnectConfig != enums:ConnectConfig.PC', Source={x:Static settings_vms:ConnectSettingsUserControlModel.Instance}}" />
                <controls:TextBlock
                    Margin="3"
                    Block.TextAlignment="Center"
                    Text="{DynamicResource WaitForPcClient}"
                    Visibility="{c:Binding 'ConnectConfig == enums:ConnectConfig.PC', Source={x:Static settings_vms:ConnectSettingsUserControlModel.Instance}}" />
                <TextBox
                    Width="50"
                    Margin="3"
                    InputMethod.IsInputMethodEnabled="False"
                    Text="{Binding EmulatorWaitSeconds}" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
