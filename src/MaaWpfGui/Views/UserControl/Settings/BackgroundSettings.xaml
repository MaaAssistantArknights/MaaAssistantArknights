<UserControl
    x:Class="MaaWpfGui.Views.UserControl.Settings.BackgroundSettingsUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:c="clr-namespace:CalcBinding;assembly=CalcBinding"
    xmlns:controls="clr-namespace:MaaWpfGui.Styles.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:helper="clr-namespace:MaaWpfGui.Helper"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:s="https://github.com/canton7/Stylet"
    xmlns:settings_vms="clr-namespace:MaaWpfGui.ViewModels.UserControl.Settings"
    d:Background="White"
    d:DataContext="{d:DesignInstance {x:Type settings_vms:BackgroundSettingsUserControlModel}}"
    d:DesignHeight="350"
    d:DesignWidth="550"
    s:View.ActionTarget="{Binding}"
    mc:Ignorable="d">
    <StackPanel>
        <controls:GlobalConfigTip />
        <StackPanel Margin="5">
            <controls:TextBlock
                Margin="0,0,0,5"
                HorizontalAlignment="Center"
                Text="{DynamicResource BackgroundImage}" />
            <Grid
                x:Name="BackgroundPathRow"
                Width="400"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <controls:TreeComboBox
                    x:Name="BackgroundImageTreeBox"
                    Grid.Column="0"
                    CornerRadius="4,0,0,4"
                    DropDownOpening="BackgroundImageTreeBox_DropDownOpening"
                    ItemsSource="{Binding BackgroundImageItems}"
                    PopupPlacementRectangle="0,30,0,0"
                    PopupPlacementTarget="{Binding ElementName=BackgroundPathRow}"
                    PopupWidth="{Binding ActualWidth, ElementName=BackgroundPathRow}"
                    SelectionChanged="BackgroundImageTreeBox_SelectionChanged"
                    Text="{Binding BackgroundImagePath}">
                    <controls:TreeComboBox.ItemTemplate>
                        <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                            <StackPanel
                                Margin="2,1"
                                HorizontalAlignment="Left"
                                Orientation="Horizontal">
                                <!--  图片项显示小预览；文件夹显示图标占位，保持对齐  -->
                                <Border
                                    Width="40"
                                    Height="28"
                                    Margin="0,0,8,0"
                                    Background="{DynamicResource RegionBrushOpacity25}"
                                    BorderBrush="{DynamicResource BorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="3"
                                    SnapsToDevicePixels="True">
                                    <Grid>
                                        <Path
                                            Width="14"
                                            Height="14"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Data="{StaticResource FolderOpen20Regular}"
                                            Fill="{DynamicResource SecondaryTextBrush}"
                                            Stretch="Uniform"
                                            Visibility="{c:Binding IsFolder}" />
                                        <Image
                                            Margin="1"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            RenderOptions.BitmapScalingMode="HighQuality"
                                            Source="{Binding Thumbnail}"
                                            Stretch="UniformToFill"
                                            Visibility="{c:Binding !IsFolder}" />
                                    </Grid>
                                </Border>
                                <controls:TextBlock VerticalAlignment="Center" Text="{Binding Name}" />
                            </StackPanel>
                        </HierarchicalDataTemplate>
                    </controls:TreeComboBox.ItemTemplate>
                    <controls:TreeComboBox.ItemContainerStyle>
                        <Style BasedOn="{StaticResource FileTreeViewItemStyle}" TargetType="{x:Type TreeViewItem}" />
                    </controls:TreeComboBox.ItemContainerStyle>
                </controls:TreeComboBox>
                <Button
                    Grid.Column="1"
                    Height="30"
                    MinWidth="30"
                    Padding="0,5"
                    hc:BorderElement.CornerRadius="0,4,4,0"
                    hc:IconElement.Geometry="{StaticResource FolderOpen20Regular}"
                    BorderThickness="0,1,1,1"
                    Command="{s:Action SelectImagePath}"
                    ToolTip="{DynamicResource Select}" />
            </Grid>
        </StackPanel>
        <StackPanel
            Margin="5"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
            <controls:TextBlock
                Margin="0,0,0,5"
                HorizontalAlignment="Center"
                Text="{DynamicResource BackgroundOpacity}" />
            <hc:PreviewSlider
                Width="400"
                d:Value="50"
                Focusable="False"
                Maximum="100"
                PreviewMouseUp="{s:Action PreviewSlider_MouseUp}"
                Value="{Binding BackgroundOpacity}">
                <hc:PreviewSlider.PreviewContent>
                    <Label Content="{c:Binding Path=(hc:PreviewSlider.PreviewPosition), RelativeSource={RelativeSource Self}}" ContentStringFormat="α: {0:F0}%" />
                </hc:PreviewSlider.PreviewContent>
            </hc:PreviewSlider>
        </StackPanel>
        <StackPanel
            Margin="5"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
            <controls:TextBlock
                Margin="0,0,0,5"
                HorizontalAlignment="Center"
                Text="{DynamicResource BackgroundBlurRadius}" />
            <hc:PreviewSlider
                Width="400"
                d:Value="5"
                Focusable="False"
                Maximum="50"
                PreviewMouseUp="{s:Action PreviewSlider_MouseUp}"
                Value="{Binding BackgroundBlurEffectRadius}">
                <hc:PreviewSlider.PreviewContent>
                    <Label Content="{c:Binding Path=(hc:PreviewSlider.PreviewPosition), RelativeSource={RelativeSource Self}}" ContentStringFormat="R: {0:F0}px" />
                </hc:PreviewSlider.PreviewContent>
            </hc:PreviewSlider>
        </StackPanel>
        <StackPanel
            Margin="5"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
            <controls:TextBlock
                Margin="0,0,0,5"
                HorizontalAlignment="Center"
                Text="{DynamicResource BackgroundImageStretchMode}" />
            <ComboBox
                Width="200"
                HorizontalContentAlignment="Stretch"
                DisplayMemberPath="Display"
                ItemsSource="{Binding BackgroundImageStretchModeList}"
                SelectedValue="{Binding BackgroundImageStretchMode}"
                SelectedValuePath="Value" />
        </StackPanel>
        <StackPanel HorizontalAlignment="Center">
            <CheckBox
                Margin="5"
                HorizontalAlignment="Center"
                Content="{DynamicResource BackgroundMonet}"
                IsChecked="{Binding BackgroundMonetEnabled}" />
            <StackPanel
                Margin="5"
                IsEnabled="{Binding BackgroundMonetEnabled}"
                Visibility="{c:Binding BackgroundMonetEnabled}">
                <!--  取色模式选择  -->
                <controls:TextBlock
                    Margin="0,0,0,5"
                    HorizontalAlignment="Center"
                    Text="{DynamicResource BackgroundMonetMode}" />
                <hc:ComboBox
                    Width="200"
                    Margin="0,0,0,5"
                    HorizontalContentAlignment="Stretch"
                    DisplayMemberPath="Display"
                    ItemsSource="{Binding BackgroundMonetModeList}"
                    SelectedValue="{Binding BackgroundMonetMode}"
                    SelectedValuePath="Value" />

                <!--  自动模式提示  -->
                <controls:TextBlock
                    HorizontalAlignment="Center"
                    FontSize="11"
                    Opacity="0.6"
                    Text="{DynamicResource BackgroundMonetAutoTip}"
                    TextWrapping="Wrap"
                    Visibility="{c:Binding IsBackgroundMonetAuto}" />

                <!--  自定义颜色选择  -->
                <StackPanel
                    HorizontalAlignment="Center"
                    Orientation="Horizontal"
                    Visibility="{c:Binding IsBackgroundMonetCustom}">
                    <!--  色块预览  -->
                    <Rectangle
                        Width="32"
                        Height="32"
                        Margin="0,0,10,0"
                        VerticalAlignment="Center"
                        RadiusX="4"
                        RadiusY="4"
                        Stroke="{DynamicResource RegionBrushOpacity50}">
                        <Rectangle.Fill>
                            <SolidColorBrush d:Color="{DynamicResource PrimaryColor}" Color="{Binding CurrentMonetColor}" />
                        </Rectangle.Fill>
                    </Rectangle>
                    <Button
                        hc:BorderElement.CornerRadius="4"
                        Command="{s:Action SelectMonetColor}"
                        Content="{DynamicResource BackgroundMonetSelectColor}" />
                </StackPanel>
            </StackPanel>
        </StackPanel>
    </StackPanel>
</UserControl>
