<UserControl
    x:Class="MaaWpfGui.Views.UserControl.Settings.ConnectSettingsUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:c="clr-namespace:CalcBinding;assembly=CalcBinding"
    xmlns:controls="clr-namespace:MaaWpfGui.Styles.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dd="urn:gong-wpf-dragdrop"
    xmlns:enums="clr-namespace:MaaWpfGui.Constants.Enums"
    xmlns:extra="clr-namespace:MaaWpfGui.Models.EmulatorConnectionExtra"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:helper="clr-namespace:MaaWpfGui.Helper"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:s="https://github.com/canton7/Stylet"
    xmlns:settings_vms="clr-namespace:MaaWpfGui.ViewModels.UserControl.Settings"
    d:Background="White"
    d:DataContext="{d:DesignInstance {x:Type settings_vms:ConnectSettingsUserControlModel}}"
    d:DesignWidth="550"
    s:View.ActionTarget="{Binding}"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel
            Grid.Row="0"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            d:Visibility="Visible"
            Orientation="Horizontal"
            Visibility="{c:Binding 'ConnectConfig != enums:ConnectConfig.PC'}">
            <StackPanel Margin="5" Orientation="Horizontal">
                <CheckBox
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Content="{DynamicResource AutoDetectConnection}"
                    IsChecked="{Binding AutoDetectConnection}" />
                <controls:TooltipBlock TooltipText="{DynamicResource AutoDetectConnectionTip}" />
            </StackPanel>
            <StackPanel
                Margin="5"
                Orientation="Horizontal"
                Visibility="{c:Binding AutoDetectConnection}">
                <CheckBox
                    Content="{DynamicResource AlwaysAutoDetectConnection}"
                    IsChecked="{Binding AlwaysAutoDetectConnection}"
                    Style="{StaticResource ErrorHighlightCheckBoxStyle}" />
                <controls:TooltipBlock TooltipText="{DynamicResource AlwaysAutoDetectConnectionTip}" />
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="1" HorizontalAlignment="Center">
            <hc:ComboBox
                Width="300"
                Height="30"
                Margin="10"
                HorizontalAlignment="Center"
                hc:TitleElement.Title="{DynamicResource ConnectionPreset}"
                hc:TitleElement.TitlePlacement="Left"
                DisplayMemberPath="Display"
                ItemsSource="{Binding ConnectConfigList}"
                SelectedValue="{Binding ConnectConfig}"
                SelectedValuePath="Value" />
            <StackPanel
                Grid.Row="0"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
                <TextBlock
                    Margin="10,0"
                    HorizontalAlignment="Center"
                    d:Visibility="Visible"
                    Foreground="Red"
                    Text="{DynamicResource UseAttachWindowWarning}"
                    TextWrapping="Wrap"
                    Visibility="{c:Binding 'ConnectConfig == enums:ConnectConfig.PC'}" />
            </StackPanel>

            <!--  ADB 连接配置区域  -->
            <StackPanel d:Visibility="Visible" Visibility="{c:Binding 'ConnectConfig != enums:ConnectConfig.PC'}">
                <StackPanel
                    Margin="10"
                    HorizontalAlignment="Center"
                    Orientation="Horizontal">
                    <controls:TooltipBlock Visibility="Hidden" />
                    <hc:ComboBox
                        Width="300"
                        HorizontalContentAlignment="Stretch"
                        hc:TitleElement.Title="{DynamicResource ConnectionAddress}"
                        hc:TitleElement.TitlePlacement="Left"
                        IsEditable="True"
                        IsEnabled="{c:Binding !AutoDetectConnection}"
                        ItemsSource="{Binding ConnectAddressHistory}"
                        Text="{Binding ConnectAddress, UpdateSourceTrigger=LostFocus}">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock
                                        Grid.Column="0"
                                        HorizontalAlignment="Left"
                                        VerticalAlignment="Center"
                                        Text="{Binding}" />
                                    <Button
                                        Grid.Column="1"
                                        Width="30"
                                        Height="30"
                                        Padding="0"
                                        HorizontalAlignment="Right"
                                        hc:IconElement.Geometry="{StaticResource DeleteTaskGeometry}"
                                        hc:IconElement.Height="15"
                                        hc:IconElement.Width="15"
                                        Command="{s:Action RemoveAddressClick}"
                                        CommandParameter="{Binding}"
                                        FontWeight="Bold" />
                                </Grid>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </hc:ComboBox>
                    <controls:TooltipBlock TooltipText="{DynamicResource ConnectionAddressTip}" />
                </StackPanel>
                <StackPanel
                    Margin="10"
                    HorizontalAlignment="Center"
                    IsEnabled="{c:Binding !AutoDetectConnection}"
                    Orientation="Horizontal">
                    <controls:TooltipBlock Visibility="Hidden" />
                    <Grid Width="420">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <hc:TextBox
                            Height="30"
                            hc:BorderElement.CornerRadius="4,0,0,4"
                            hc:TitleElement.Title="{DynamicResource AdbPath}"
                            hc:TitleElement.TitlePlacement="Left"
                            Text="{Binding AdbPath}" />
                        <Button
                            Grid.Column="1"
                            Width="60"
                            Height="30"
                            HorizontalAlignment="Right"
                            hc:BorderElement.CornerRadius="0,4,4,0"
                            BorderThickness="0,1,1,1"
                            Command="{s:Action SelectFile}"
                            Content="{DynamicResource Select}" />
                    </Grid>
                    <controls:TooltipBlock Visibility="Hidden" />
                </StackPanel>
            </StackPanel>

        </StackPanel>

        <StackPanel Grid.Row="2" IsEnabled="{c:Binding !AutoDetectConnection}">
            <StackPanel>
                <ContentControl d:Content="{d:DesignInstance Type={x:Type extra:MuMu12Extra}}" Content="{Binding ExtraConfig}">
                    <ContentControl.Resources>
                        <DataTemplate DataType="{x:Type extra:MuMu12Extra}">
                            <StackPanel d:DataContext="{d:DesignInstance {x:Type extra:MuMu12Extra}}">
                                <CheckBox
                                    Margin="10"
                                    HorizontalAlignment="Center"
                                    Content="{DynamicResource MuMu12ExtrasEnabled}"
                                    IsChecked="{Binding Enable}" />
                                <StackPanel
                                    HorizontalAlignment="Center"
                                    d:Visibility="Visible"
                                    Orientation="Vertical"
                                    Visibility="{c:Binding Enable}">
                                    <hc:TextBox
                                        Width="420"
                                        Height="30"
                                        Margin="10"
                                        hc:InfoElement.Placeholder="Example: C:\Program Files\Netease\MuMuPlayer"
                                        hc:TitleElement.Title="{DynamicResource MuMu12EmulatorPath}"
                                        hc:TitleElement.TitlePlacement="Left"
                                        Text="{Binding EmulatorPath}" />
                                    <CheckBox
                                        Margin="10"
                                        HorizontalAlignment="Center"
                                        Content="{DynamicResource MuMuExtrasTouchEnabled}"
                                        IsChecked="{Binding EnableTouch}" />
                                    <StackPanel HorizontalAlignment="Center" Orientation="Vertical">
                                        <CheckBox
                                            Margin="10"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Content="{DynamicResource MuMuBridgeConnection}"
                                            IsChecked="{Binding EnableBridgeConnection}" />
                                        <hc:TextBox
                                            Height="30"
                                            Margin="10"
                                            HorizontalContentAlignment="Center"
                                            d:Visibility="Visible"
                                            hc:TitleElement.Title="{DynamicResource MuMu12Index}"
                                            hc:TitleElement.TitlePlacement="Left"
                                            Text="{Binding InstanceIndex}"
                                            Visibility="{c:Binding EnableBridgeConnection}" />
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                        <DataTemplate DataType="{x:Type extra:LDPlayerExtra}">
                            <StackPanel d:DataContext="{d:DesignInstance {x:Type extra:LDPlayerExtra}}">
                                <CheckBox
                                    Margin="10"
                                    HorizontalAlignment="Center"
                                    Content="{DynamicResource LdExtrasEnabled}"
                                    IsChecked="{Binding Enable}" />
                                <StackPanel d:Visibility="Visible" Visibility="{c:Binding Enable}">
                                    <hc:TextBox
                                        Width="420"
                                        Height="30"
                                        Margin="10"
                                        hc:InfoElement.Placeholder="Example: C:\leidian\LDPlayer9"
                                        hc:TitleElement.Title="{DynamicResource LdEmulatorPath}"
                                        hc:TitleElement.TitlePlacement="Left"
                                        Text="{Binding EmulatorPath}" />
                                    <StackPanel>
                                        <CheckBox
                                            HorizontalAlignment="Center"
                                            VerticalContentAlignment="Center"
                                            Content="{DynamicResource LdManualSetIndex}"
                                            IsChecked="{Binding ManualSetIndex}" />
                                        <hc:TextBox
                                            Width="130"
                                            Height="30"
                                            Margin="10"
                                            d:Visibility="Visible"
                                            hc:InfoElement.Placeholder="0"
                                            hc:TitleElement.Title="{DynamicResource LdIndex}"
                                            hc:TitleElement.TitlePlacement="Left"
                                            Text="{Binding InstanceIndex}"
                                            Visibility="{c:Binding ManualSetIndex}" />
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                        <DataTemplate DataType="{x:Type extra:Win32Extra}">
                            <StackPanel d:DataContext="{d:DesignInstance {x:Type extra:Win32Extra}}">
                                <StackPanel
                                    Margin="10"
                                    HorizontalAlignment="Center"
                                    Orientation="Horizontal">
                                    <hc:ComboBox
                                        Width="300"
                                        Height="30"
                                        hc:TitleElement.Title="{DynamicResource AttachWindowScreencapMethod}"
                                        hc:TitleElement.TitlePlacement="Left"
                                        DisplayMemberPath="Display"
                                        ItemsSource="{Binding ScreencapMethodList}"
                                        SelectedValue="{Binding ScreencapMethod}"
                                        SelectedValuePath="Value" />
                                </StackPanel>

                                <StackPanel
                                    Margin="10"
                                    HorizontalAlignment="Center"
                                    Orientation="Horizontal">
                                    <hc:ComboBox
                                        Width="300"
                                        Height="30"
                                        hc:TitleElement.Title="{DynamicResource AttachWindowMouseMethod}"
                                        hc:TitleElement.TitlePlacement="Left"
                                        DisplayMemberPath="Display"
                                        ItemsSource="{Binding MouseMethodList}"
                                        SelectedValue="{Binding MouseMethod}"
                                        SelectedValuePath="Value" />
                                </StackPanel>

                                <StackPanel
                                    Margin="10"
                                    HorizontalAlignment="Center"
                                    Orientation="Horizontal">
                                    <hc:ComboBox
                                        Width="300"
                                        Height="30"
                                        hc:TitleElement.Title="{DynamicResource AttachWindowKeyboardMethod}"
                                        hc:TitleElement.TitlePlacement="Left"
                                        DisplayMemberPath="Display"
                                        ItemsSource="{Binding KeyboardMethodList}"
                                        SelectedValue="{Binding KeyboardMethod}"
                                        SelectedValuePath="Value" />
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </ContentControl.Resources>
                </ContentControl>
            </StackPanel>
        </StackPanel>

        <StackPanel
            Grid.Row="3"
            HorizontalAlignment="Center"
            Orientation="Vertical">
            <Button
                Height="30"
                Margin="5"
                HorizontalAlignment="Center"
                Command="{s:Action TestLinkAndGetImage}"
                Content="{DynamicResource ScreenshotTest}" />
            <controls:TextBlock
                Margin="5"
                HorizontalAlignment="Center"
                Text="{Binding TestLinkInfo}"
                TextWrapping="Wrap"
                Visibility="{c:Binding 'TestLinkInfo!=&quot;&quot;'}" />
        </StackPanel>

        <controls:TextBlock
            Grid.Row="4"
            Margin="10"
            HorizontalAlignment="Center"
            Text="{c:Binding ScreencapCost}" />

        <StackPanel
            Grid.Row="5"
            HorizontalAlignment="Center"
            d:Visibility="Visible"
            Visibility="{c:Binding 'ConnectConfig != enums:ConnectConfig.PC'}">
            <StackPanel Orientation="Horizontal">
                <CheckBox
                    Margin="20,10,0,10"
                    Content="{DynamicResource RetryOnDisconnected}"
                    IsChecked="{Binding RetryOnDisconnected}" />
                <controls:TooltipBlock TooltipText="{DynamicResource RetryOnDisconnectedTip}" />
            </StackPanel>
            <CheckBox
                Margin="20,10"
                Content="{DynamicResource AllowAdbRestart}"
                IsChecked="{Binding AllowAdbRestart}" />
            <CheckBox
                Margin="20,10"
                Content="{DynamicResource AllowAdbHardRestart}"
                IsChecked="{Binding AllowAdbHardRestart}" />
        </StackPanel>

        <StackPanel
            Grid.Row="6"
            HorizontalAlignment="Center"
            d:Visibility="Visible"
            Orientation="Horizontal"
            Visibility="{c:Binding 'ConnectConfig != enums:ConnectConfig.PC'}">
            <hc:ComboBox
                Height="30"
                Margin="10,10,0,10"
                hc:TitleElement.Title="{DynamicResource TouchMode}"
                hc:TitleElement.TitlePlacement="Left"
                DisplayMemberPath="Display"
                IsHitTestVisible="{Binding Idle, Source={x:Static helper:Instances.SettingsViewModel}}"
                ItemsSource="{Binding TouchModeList}"
                SelectedValue="{Binding TouchMode}"
                SelectedValuePath="Value" />
            <controls:TooltipBlock>
                <controls:TooltipBlock.CustomToolTip>
                    <StackPanel MaxWidth="400">
                        <MediaElement
                            Width="252"
                            Height="268"
                            IsMuted="True"
                            LoadedBehavior="Play"
                            Source="{Binding Source={x:Static settings_vms:ConnectSettingsUserControlModel.TouchModeVideoPath}}" />
                        <TextBlock
                            Margin="0,8,0,0"
                            FontSize="12"
                            Foreground="{DynamicResource PrimaryTextBrush}"
                            Text="{DynamicResource TouchModeTip}"
                            TextWrapping="Wrap" />
                    </StackPanel>
                </controls:TooltipBlock.CustomToolTip>
            </controls:TooltipBlock>
            <Button
                Height="30"
                Margin="10,10,0,10"
                HorizontalAlignment="Left"
                Command="{s:Action ReplaceAdb}"
                Content="{DynamicResource ForcedReplaceAdb}" />
            <controls:TooltipBlock TooltipText="{DynamicResource ForcedReplaceAdbTip}" />
        </StackPanel>

        <StackPanel
            Grid.Row="7"
            d:Visibility="Visible"
            Visibility="{c:Binding 'ConnectConfig != enums:ConnectConfig.PC'}">
            <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                <CheckBox
                    Margin="20,10"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Content="{DynamicResource KillAdbOnExit}"
                    IsChecked="{Binding KillAdbOnExit}" />
                <CheckBox
                    Margin="20,10"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Content="{DynamicResource AdbLiteEnabled}"
                    IsChecked="{Binding AdbLiteEnabled}" />
            </StackPanel>
        </StackPanel>

        <StackPanel
            Grid.Row="8"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Orientation="Horizontal" />
    </Grid>
</UserControl>
