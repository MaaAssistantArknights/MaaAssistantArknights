<UserControl
    x:Class="MaaWpfGui.Views.UserControl.TaskQueue.InfrastSettingsUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:c="clr-namespace:CalcBinding;assembly=CalcBinding"
    xmlns:constants="clr-namespace:MaaWpfGui.Constants"
    xmlns:controls="clr-namespace:MaaWpfGui.Styles.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dd="urn:gong-wpf-dragdrop"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:helper="clr-namespace:MaaWpfGui.Helper"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:s="https://github.com/canton7/Stylet"
    xmlns:task="clr-namespace:MaaWpfGui.Configuration.Single.MaaTask"
    xmlns:taskQueue_vms="clr-namespace:MaaWpfGui.ViewModels.UserControl.TaskQueue"
    xmlns:ui_vms="clr-namespace:MaaWpfGui.ViewModels.UI"
    d:Background="White"
    d:DataContext="{d:DesignInstance {x:Type taskQueue_vms:InfrastSettingsUserControlModel}}"
    d:DesignWidth="220"
    s:View.ActionTarget="{Binding}"
    mc:Ignorable="d">
    <StackPanel>
        <!--  常规设置  -->
        <StackPanel Visibility="{c:Binding '!TaskSettingVisibilities.EnableAdvancedSettings'}">
            <StackPanel Margin="0,4">
                <StackPanel Margin="0,0,0,5" Orientation="Horizontal">
                    <controls:TextBlock
                        Margin="8,0,0,0"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        Text="{DynamicResource InfrastMode}" />
                    <controls:TooltipBlock
                        VerticalAlignment="Center"
                        d:Visibility="Visible"
                        TooltipText="{DynamicResource InfrastModeRotationToolTip}"
                        Visibility="{c:Binding 'InfrastMode == taskQueue_vms:InfrastMode.Rotation',
                                               Source={x:Static taskQueue_vms:InfrastSettingsUserControlModel.Instance}}" />
                </StackPanel>
                <ComboBox
                    VerticalContentAlignment="Center"
                    DisplayMemberPath="Display"
                    ItemsSource="{Binding InfrastModeList}"
                    SelectedValue="{Binding InfrastMode}"
                    SelectedValuePath="Value" />
            </StackPanel>
            <StackPanel d:Visibility="Visible" Visibility="{c:Binding 'InfrastMode == taskQueue_vms:InfrastMode.Custom', Source={x:Static taskQueue_vms:InfrastSettingsUserControlModel.Instance}}">
                <StackPanel Margin="0,4">
                    <StackPanel Margin="0,0,0,5" Orientation="Horizontal">
                        <controls:TextBlock
                            Margin="8,0,0,0"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Text="{DynamicResource CustomInfrastPlan}" />
                        <controls:TooltipBlock
                            VerticalAlignment="Center"
                            d:Visibility="Visible"
                            TooltipText="{DynamicResource CustomInfrastPlanToolTip}" />
                    </StackPanel>
                    <ComboBox
                        Margin="0,4"
                        VerticalContentAlignment="Center"
                        d:IsHitTestVisible="True"
                        DisplayMemberPath="Display"
                        IsHitTestVisible="{c:Binding !IsCurrentTaskRunning,
                                                     Source={x:Static ui_vms:TaskQueueViewModel.TaskSettingVisibilities}}"
                        ItemsSource="{Binding CustomPlanListDisplay}"
                        SelectedValue="{Binding CustomInfrastPlanSelect}"
                        SelectedValuePath="Value" />
                </StackPanel>
                <controls:TextBlock
                    Margin="0,5"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                    <Hyperlink
                        d:NavigateUri="Null"
                        Cursor="Hand"
                        NavigateUri="{Binding Source={x:Static constants:MaaUrls.CustomInfrastGenerator}}"
                        TextDecorations="None">
                        <TextBlock Style="{StaticResource HyperlinkTextBlockStyle}" Text="{DynamicResource CustomInfrastGenerator}" />
                    </Hyperlink>
                </controls:TextBlock>
                <StackPanel Orientation="Horizontal">
                    <ComboBox
                        Width="150"
                        Margin="0,5"
                        hc:InfoElement.Title="{DynamicResource DefaultInfrast}"
                        DisplayMemberPath="Display"
                        ItemsSource="{Binding DefaultInfrastList}"
                        SelectedValue="{Binding DefaultInfrast}"
                        SelectedValuePath="Value" />
                    <Button
                        Width="60"
                        Margin="10,5"
                        VerticalAlignment="Bottom"
                        Command="{s:Action SelectCustomInfrastFile}"
                        Content="{DynamicResource Select}" />
                </StackPanel>
                <TextBox
                    Margin="0,5"
                    IsReadOnly="{Binding IsCustomInfrastFileReadOnly}"
                    Text="{Binding CustomInfrastFile}" />
            </StackPanel>
            <ComboBox
                Margin="0,5"
                d:IsEnabled="True"
                hc:InfoElement.Title="{DynamicResource DroneUsage}"
                DisplayMemberPath="Display"
                IsEnabled="{c:Binding 'InfrastMode != taskQueue_vms:InfrastMode.Custom',
                                      Source={x:Static taskQueue_vms:InfrastSettingsUserControlModel.Instance}}"
                ItemsSource="{Binding UsesOfDronesList}"
                SelectedValue="{Binding UsesOfDrones}"
                SelectedValuePath="Value" />
            <controls:TextBlock
                Width="220"
                Margin="0,4"
                d:Visibility="Visible"
                Text="{DynamicResource InfrastModeRotationTip}"
                TextWrapping="Wrap"
                Visibility="{c:Binding 'InfrastMode == taskQueue_vms:InfrastMode.Rotation',
                                       Source={x:Static taskQueue_vms:InfrastSettingsUserControlModel.Instance}}" />
            <StackPanel
                Margin="0"
                d:Visibility="Visible"
                Visibility="{c:Binding 'InfrastMode != taskQueue_vms:InfrastMode.Rotation',
                                       Source={x:Static taskQueue_vms:InfrastSettingsUserControlModel.Instance}}">
                <StackPanel
                    Margin="0,5"
                    HorizontalAlignment="Center"
                    Orientation="Horizontal">
                    <controls:TextBlock
                        MaxWidth="150"
                        Text="{DynamicResource DormThreshold}"
                        TextWrapping="Wrap" />
                    <controls:TextBlock Text=": " />
                    <controls:TextBlock d:Text="30" Text="{Binding DormThreshold}" />
                    <controls:TextBlock Text="%" />
                    <controls:TooltipBlock TooltipText="{DynamicResource InfrastThresholdTip}" />
                </StackPanel>
                <Slider
                    Margin="0,5"
                    Maximum="100"
                    Minimum="0"
                    Value="{Binding DormThreshold}" />
            </StackPanel>
            <StackPanel>
                <ListBox
                    Margin="0,5"
                    d:ItemsSource="{d:SampleData ItemCount=3}"
                    ItemsSource="{Binding Path=InfrastRoomModels}"
                    PreviewMouseWheel="{s:Action RouteMouseWheelToParent,
                                                 Target={x:Type helper:MouseWheelHelper}}"
                    Style="{DynamicResource NoSelectedHighlightListBoxStyle}"
                    Visibility="{c:Binding 'InfrastMode == taskQueue_vms:InfrastMode.Normal',
                                           Source={x:Static taskQueue_vms:InfrastSettingsUserControlModel.Instance}}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding Name}" IsChecked="{Binding IsEnabled}" />
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <ListBox
                    Margin="0,5"
                    d:ItemsSource="{d:SampleData ItemCount=3}"
                    dd:DragDrop.DragDropContext="{Binding RelativeSource={RelativeSource Self}}"
                    dd:DragDrop.IsDragSource="True"
                    dd:DragDrop.IsDropTarget="True"
                    ItemsSource="{Binding Path=InfrastRoomModels}"
                    PreviewMouseWheel="{s:Action RouteMouseWheelToParent,
                                                 Target={x:Type helper:MouseWheelHelper}}"
                    Style="{DynamicResource NoSelectedHighlightListBoxStyle}"
                    ToolTip="{DynamicResource LabelSequenceTip}"
                    Visibility="{c:Binding 'InfrastMode != taskQueue_vms:InfrastMode.Normal',
                                           Source={x:Static taskQueue_vms:InfrastSettingsUserControlModel.Instance}}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <CheckBox Content="{Binding Name}" IsChecked="{Binding IsEnabled}" />
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Button
                        Height="30"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Top"
                        Command="{s:Action InfrastItemSelectedAll}"
                        Content="{DynamicResource SelectAll}"
                        IsEnabled="{Binding Idle, Source={x:Static ui_vms:TaskQueueViewModel.TaskSettingDataContext}}" />
                    <Button
                        Grid.Column="1"
                        Height="30"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Top"
                        Command="{s:Action InfrastItemUnselectedAll}"
                        Content="{DynamicResource Clear}"
                        IsEnabled="{Binding Idle, Source={x:Static ui_vms:TaskQueueViewModel.TaskSettingDataContext}}" />
                </Grid>
            </StackPanel>
        </StackPanel>

        <!--  高级设置  -->
        <StackPanel d:Visibility="Visible" Visibility="{c:Binding EnableAdvancedSettings, Source={x:Static ui_vms:TaskQueueViewModel.TaskSettingVisibilities}}">
            <CheckBox
                Margin="0,10"
                d:Visibility="Visible"
                Content="{DynamicResource DormTrustEnabled}"
                IsChecked="{Binding DormTrustEnabled}"
                Visibility="{c:Binding 'InfrastMode != taskQueue_vms:InfrastMode.Rotation',
                                       Source={x:Static taskQueue_vms:InfrastSettingsUserControlModel.Instance}}" />
            <StackPanel
                Margin="0,10"
                d:Visibility="Visible"
                Orientation="Horizontal"
                Visibility="{c:Binding 'InfrastMode != taskQueue_vms:InfrastMode.Rotation',
                                       Source={x:Static taskQueue_vms:InfrastSettingsUserControlModel.Instance}}">
                <CheckBox MaxWidth="{c:Binding '220 - ActualWidth', ElementName=DormFilterNotStationedTipsTooltipBlock}" IsChecked="{c:Binding DormFilterNotStationedEnabled}">
                    <controls:TextBlock Text="{DynamicResource DormFilterNotStationedEnabled}" TextWrapping="Wrap" />
                </CheckBox>
                <controls:TooltipBlock x:Name="DormFilterNotStationedTipsTooltipBlock" TooltipText="{DynamicResource DormFilterNotStationedTips}" />
            </StackPanel>
            <CheckBox Margin="0,10" IsChecked="{Binding OriginiumShardAutoReplenishment}">
                <controls:TextBlock Text="{DynamicResource OriginiumShardAutoReplenishment}" TextWrapping="Wrap" />
            </CheckBox>
            <CheckBox Margin="0,10" IsChecked="{Binding ReceptionMessageBoardReceive}">
                <controls:TextBlock Text="{DynamicResource InfrastReceptionMessageBoardReceive}" TextWrapping="Wrap" />
            </CheckBox>
            <StackPanel Margin="0,10" Orientation="Horizontal">
                <CheckBox MaxWidth="{c:Binding '220 - ActualWidth', ElementName=ReceptionClueExchangeTipsTooltipBlock}" IsChecked="{Binding ReceptionClueExchange}">
                    <controls:TextBlock Text="{DynamicResource InfrastReceptionClueExchange}" TextWrapping="Wrap" />
                </CheckBox>
                <controls:TooltipBlock x:Name="ReceptionClueExchangeTipsTooltipBlock" TooltipText="{DynamicResource InfrastReceptionClueExchangeTips}" />
            </StackPanel>
            <CheckBox Margin="0,10" IsChecked="{Binding ReceptionSendClue}">
                <controls:TextBlock Text="{DynamicResource InfrastReceptionSendClue}" TextWrapping="Wrap" />
            </CheckBox>
            <CheckBox Margin="0,10" IsChecked="{Binding ContinueTraining}">
                <controls:TextBlock Text="{DynamicResource ContinueTraining}" TextWrapping="Wrap" />
            </CheckBox>
            <StackPanel
                Margin="0,5"
                HorizontalAlignment="Left"
                Visibility="{c:Binding 'InfrastMode == taskQueue_vms:InfrastMode.Normal',
                                       Source={x:Static taskQueue_vms:InfrastSettingsUserControlModel.Instance}}">
                <StackPanel.Resources>
                    <DataTemplate x:Key="FiammettaTargetTemplate">
                        <TextBlock
                            Text="{Binding Display}"
                            TextTrimming="CharacterEllipsis"
                            ToolTip="{Binding Display}" />
                    </DataTemplate>
                </StackPanel.Resources>
                <StackPanel Margin="0,0,0,5" HorizontalAlignment="Left" Orientation="Horizontal">
                    <controls:TextBlock
                        Margin="8,0,0,0"
                        HorizontalAlignment="Left"
                        Text="{DynamicResource InfrastFiammettaTargets}"
                        TextAlignment="Left"
                        TextWrapping="Wrap" />
                    <controls:TooltipBlock TooltipText="{DynamicResource InfrastFiammettaTargetsTips}" />
                </StackPanel>
                <Grid Width="220" HorizontalAlignment="Left">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <ComboBox
                        Grid.Column="0"
                        MinWidth="0"
                        Margin="0,3,3,3"
                        FontSize="12"
                        HorizontalContentAlignment="Stretch"
                        ItemTemplate="{StaticResource FiammettaTargetTemplate}"
                        ItemsSource="{Binding FiammettaTargetList}"
                        SelectedValue="{Binding FiammettaTarget1}"
                        SelectedValuePath="Value" />
                    <ComboBox
                        Grid.Column="1"
                        MinWidth="0"
                        Margin="3"
                        FontSize="12"
                        HorizontalContentAlignment="Stretch"
                        ItemTemplate="{StaticResource FiammettaTargetTemplate}"
                        ItemsSource="{Binding OptionalFiammettaTargetList}"
                        SelectedValue="{Binding FiammettaTarget2}"
                        SelectedValuePath="Value" />
                    <ComboBox
                        Grid.Column="2"
                        MinWidth="0"
                        Margin="3,3,0,3"
                        FontSize="12"
                        HorizontalContentAlignment="Stretch"
                        ItemTemplate="{StaticResource FiammettaTargetTemplate}"
                        ItemsSource="{Binding OptionalFiammettaTargetList}"
                        SelectedValue="{Binding FiammettaTarget3}"
                        SelectedValuePath="Value" />
                </Grid>
            </StackPanel>
            <StackPanel
                Margin="0,10"
                Orientation="Horizontal"
                Visibility="{c:Binding 'InfrastMode == taskQueue_vms:InfrastMode.Normal',
                                       Source={x:Static taskQueue_vms:InfrastSettingsUserControlModel.Instance}}">
                <CheckBox
                    MaxWidth="{c:Binding '220 - ActualWidth', ElementName=InfrastUsePinusSylvestrisTipsTooltipBlock}"
                    IsChecked="{Binding UsePinusSylvestris}">
                    <controls:TextBlock Text="{DynamicResource InfrastUsePinusSylvestris}" TextWrapping="Wrap" />
                </CheckBox>
                <controls:TooltipBlock
                    x:Name="InfrastUsePinusSylvestrisTipsTooltipBlock"
                    TooltipText="{DynamicResource InfrastUsePinusSylvestrisTips}" />
            </StackPanel>
            <StackPanel
                Margin="0,10"
                Orientation="Horizontal"
                Visibility="{c:Binding 'InfrastMode == taskQueue_vms:InfrastMode.Normal',
                                       Source={x:Static taskQueue_vms:InfrastSettingsUserControlModel.Instance}}">
                <CheckBox
                    MaxWidth="{c:Binding '220 - ActualWidth', ElementName=InfrastUsePerceptionInformationTipsTooltipBlock}"
                    IsChecked="{Binding UsePerceptionInformation}">
                    <controls:TextBlock Text="{DynamicResource InfrastUsePerceptionInformation}" TextWrapping="Wrap" />
                </CheckBox>
                <controls:TooltipBlock
                    x:Name="InfrastUsePerceptionInformationTipsTooltipBlock"
                    TooltipText="{DynamicResource InfrastUsePerceptionInformationTips}" />
            </StackPanel>
            <StackPanel
                Margin="0,10"
                Orientation="Horizontal"
                Visibility="{c:Binding 'InfrastMode == taskQueue_vms:InfrastMode.Normal',
                                       Source={x:Static taskQueue_vms:InfrastSettingsUserControlModel.Instance}}">
                <CheckBox
                    MaxWidth="{c:Binding '220 - ActualWidth', ElementName=InfrastUseWorldlyPlightTipsTooltipBlock}"
                    IsChecked="{Binding UseWorldlyPlight}">
                    <controls:TextBlock Text="{DynamicResource InfrastUseWorldlyPlight}" TextWrapping="Wrap" />
                </CheckBox>
                <controls:TooltipBlock
                    x:Name="InfrastUseWorldlyPlightTipsTooltipBlock"
                    TooltipText="{DynamicResource InfrastUseWorldlyPlightTips}" />
            </StackPanel>
            <StackPanel
                Margin="0,10"
                Orientation="Horizontal"
                Visibility="{c:Binding 'InfrastMode == taskQueue_vms:InfrastMode.Normal',
                                       Source={x:Static taskQueue_vms:InfrastSettingsUserControlModel.Instance}}">
                <CheckBox
                    MaxWidth="{c:Binding '220 - ActualWidth', ElementName=InfrastUseAbyssalHunterTipsTooltipBlock}"
                    IsChecked="{Binding UseAbyssalHunter}">
                    <controls:TextBlock Text="{DynamicResource InfrastUseAbyssalHunter}" TextWrapping="Wrap" />
                </CheckBox>
                <controls:TooltipBlock
                    x:Name="InfrastUseAbyssalHunterTipsTooltipBlock"
                    TooltipText="{DynamicResource InfrastUseAbyssalHunterTips}" />
            </StackPanel>
        </StackPanel>
    </StackPanel>
</UserControl>
