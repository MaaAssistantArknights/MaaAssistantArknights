<UserControl
    x:Class="MaaWpfGui.Views.UserControl.TaskQueue.MallSettingsUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:c="clr-namespace:CalcBinding;assembly=CalcBinding"
    xmlns:controls="clr-namespace:MaaWpfGui.Styles.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dd="urn:gong-wpf-dragdrop"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:helper="clr-namespace:MaaWpfGui.Helper"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:s="https://github.com/canton7/Stylet"
    xmlns:taskQueue_vms="clr-namespace:MaaWpfGui.ViewModels.UserControl.TaskQueue"
    xmlns:ui_vms="clr-namespace:MaaWpfGui.ViewModels.UI"
    d:Background="White"
    d:DataContext="{d:DesignInstance {x:Type taskQueue_vms:MallSettingsUserControlModel}}"
    d:DesignWidth="220"
    s:View.ActionTarget="{Binding}"
    mc:Ignorable="d">
    <StackPanel>
        <StackPanel Visibility="{c:Binding !EnableAdvancedSettings, Source={x:Static ui_vms:TaskQueueViewModel.TaskSettingVisibilities}}">
            <CheckBox
                Margin="10,10,10,5"
                Block.TextAlignment="Left"
                Content="{DynamicResource Visiting}"
                IsChecked="{Binding CreditVisitFriendsEnabled}" />
            <StackPanel
                Margin="10,0,10,0"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Orientation="Horizontal"
                Visibility="{c:Binding CreditVisitFriendsEnabled}">
                <controls:TextBlock
                    Margin="2.5,0,7.5,0"
                    Opacity="{c:Binding 'Idle and Inited ? 1 : 0.5',
                                        Source={x:Static helper:Instances.TaskQueueViewModel}}"
                    Text="└" />
                <CheckBox
                    Block.TextAlignment="Left"
                    Content="{DynamicResource OnlyOnceADay}"
                    IsChecked="{Binding CreditVisitOnceADay}"
                    Visibility="{c:Binding CreditVisitFriendsEnabled}" />
            </StackPanel>


            <StackPanel Margin="10,10,10,5" Orientation="Horizontal">
                <CheckBox
                    MaxWidth="{c:Binding '220 - ActualWidth',
                                         ElementName=CreditFightTipTooltipBlock}"
                    Block.TextAlignment="Left"
                    IsChecked="{Binding CreditFightTaskEnabled}">
                    <TextBlock
                        Block.TextAlignment="Left"
                        Text="{DynamicResource CreditFight}"
                        TextWrapping="Wrap" />
                </CheckBox>
                <controls:TooltipBlock x:Name="CreditFightTipTooltipBlock" TooltipText="{DynamicResource CreditFightTip}" />
            </StackPanel>
            <StackPanel
                Margin="10,0,10,0"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Orientation="Horizontal"
                Visibility="{c:Binding CreditFightTaskEnabled}">
                <controls:TextBlock
                    Margin="2.5,0,7.5,0"
                    Opacity="{c:Binding 'Idle and Inited ? 1 : 0.5',
                                        Source={x:Static helper:Instances.TaskQueueViewModel}}"
                    Text="├" />
                <CheckBox
                    Block.TextAlignment="Left"
                    Content="{DynamicResource OnlyOnceADay}"
                    IsChecked="{Binding CreditFightOnceADay}"
                    Visibility="{c:Binding CreditFightTaskEnabled}" />
            </StackPanel>
            <StackPanel
                Margin="10,5,10,0"
                Orientation="Horizontal"
                Visibility="{c:Binding CreditFightTaskEnabled}">
                <controls:TextBlock
                    Margin="2.5,0,7.5,0"
                    Opacity="{c:Binding 'Idle and Inited ? 1 : 0.5',
                                        Source={x:Static helper:Instances.TaskQueueViewModel}}"
                    Text="└" />
                <controls:TextBlock Opacity="{c:Binding 'Idle and Inited ? 1 : 0.5', Source={x:Static helper:Instances.TaskQueueViewModel}}" Text="{DynamicResource UseFormation}" />
                <ComboBox
                    Margin="6,0,0,0"
                    HorizontalAlignment="Left"
                    DisplayMemberPath="Display"
                    IsHitTestVisible="{Binding Idle, Source={x:Static ui_vms:TaskQueueViewModel.TaskSettingDataContext}}"
                    ItemsSource="{Binding FormationSelectList}"
                    SelectedValue="{Binding CreditFightSelectFormation}"
                    SelectedValuePath="Value" />
            </StackPanel>

            <CheckBox
                Margin="10"
                Block.TextAlignment="Left"
                Content="{DynamicResource SocialPtShop}"
                IsChecked="{Binding CreditShopping}" />

            <TextBlock
                Margin="10,0,0,5"
                Foreground="{DynamicResource ThirdlyTextBrush}"
                Text="{DynamicResource ReceptionOptionsRequireInfrast}"
                TextWrapping="Wrap" />
            <Border
                Margin="10,0"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="4">
                <StackPanel Opacity="{c:Binding 'Idle and Inited ? 1 : 0.5', Source={x:Static helper:Instances.TaskQueueViewModel}}">
                    <controls:TextBlock
                        Margin="6,8"
                        HorizontalAlignment="Stretch"
                        Text="{DynamicResource InfrastReceptionMessageBoardReceive}"
                        TextWrapping="Wrap" />
                    <controls:TextBlock
                        Margin="6,8"
                        HorizontalAlignment="Stretch"
                        Text="{DynamicResource InfrastReceptionClueExchange}"
                        TextWrapping="Wrap" />
                    <controls:TextBlock
                        Margin="6,8"
                        HorizontalAlignment="Stretch"
                        Text="{DynamicResource InfrastReceptionSendClue}"
                        TextWrapping="Wrap" />

                </StackPanel>
            </Border>
        </StackPanel>

        <StackPanel Visibility="{c:Binding EnableAdvancedSettings, Source={x:Static ui_vms:TaskQueueViewModel.TaskSettingVisibilities}}">
            <TextBox
                Margin="0,10"
                hc:InfoElement.Title="{DynamicResource HighPriority}"
                IsEnabled="{Binding CreditShopping}"
                Style="{StaticResource TextBoxExtend}"
                Text="{Binding CreditFirstList}"
                ToolTip="{DynamicResource Drink}" />
            <TextBox
                Margin="0,10"
                hc:InfoElement.Title="{DynamicResource Blacklist}"
                IsEnabled="{Binding CreditShopping}"
                Style="{StaticResource TextBoxExtend}"
                Text="{Binding CreditBlackList}" />
            <CheckBox
                Margin="0,10"
                IsChecked="{Binding CreditForceShoppingIfCreditFull}"
                IsEnabled="{Binding CreditShopping}">
                <TextBlock
                    Block.TextAlignment="Left"
                    Text="{DynamicResource ForceShoppingIfCreditFull}"
                    TextWrapping="Wrap" />
            </CheckBox>
            <StackPanel Margin="0,10" Orientation="Horizontal">
                <CheckBox
                    MaxWidth="{c:Binding '220 - ActualWidth',
                                         ElementName=OnlyBuyDiscountTipsTooltipBlock}"
                    IsChecked="{Binding CreditOnlyBuyDiscount}"
                    IsEnabled="{Binding CreditShopping}">
                    <TextBlock
                        Block.TextAlignment="Left"
                        Text="{DynamicResource OnlyBuyDiscount}"
                        TextWrapping="Wrap" />
                </CheckBox>
                <controls:TooltipBlock x:Name="OnlyBuyDiscountTipsTooltipBlock" TooltipText="{DynamicResource OnlyBuyDiscountTips}" />
            </StackPanel>
            <StackPanel Margin="0,10" Orientation="Horizontal">
                <CheckBox
                    MaxWidth="{c:Binding '220 - ActualWidth',
                                         ElementName=ReserveMaxCreditTipsTooltipBlock}"
                    IsChecked="{Binding CreditReserveMaxCredit}"
                    IsEnabled="{Binding CreditShopping}">
                    <TextBlock
                        Block.TextAlignment="Left"
                        Text="{DynamicResource ReserveMaxCredit}"
                        TextWrapping="Wrap" />
                </CheckBox>
                <controls:TooltipBlock x:Name="ReserveMaxCreditTipsTooltipBlock" TooltipText="{DynamicResource ReserveMaxCreditTips}" />
            </StackPanel>
        </StackPanel>
    </StackPanel>
</UserControl>
