<hc:Window
    x:Class="MaaWpfGui.Views.Dialogs.EditTaskDialogUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:MaaWpfGui.Styles.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="EditTaskDialogWindow"
    Title="{DynamicResource EditTask}"
    Width="400"
    Height="260"
    Background="{DynamicResource RegionBrush}"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterOwner"
    mc:Ignorable="d">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  提示文本  -->
        <controls:TextBlock
            x:Name="PromptTextBlock"
            Grid.Row="0"
            Margin="0,0,0,15"
            Text="{DynamicResource RenameTaskPrompt}"
            TextWrapping="Wrap" />

        <!--  输入框  -->
        <TextBox
            x:Name="InputTextBox"
            Grid.Row="1"
            Margin="0,0,0,10"
            VerticalAlignment="Top"
            FontSize="14" />

        <!--  名称操作按钮：应用 / 确认 / 取消  -->
        <StackPanel
            Grid.Row="2"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                Width="80"
                Height="32"
                Margin="0,0,10,0"
                Click="BtnApply_Click"
                Content="{DynamicResource EditTaskApply}" />
            <Button
                Width="80"
                Height="32"
                Margin="0,0,10,0"
                Click="BtnConfirm_Click"
                Content="{DynamicResource Confirm}"
                IsDefault="True" />
            <Button
                Width="80"
                Height="32"
                Click="BtnCancel_Click"
                Content="{DynamicResource Cancel}"
                IsCancel="True" />
        </StackPanel>

        <!--  任务操作按钮：复制任务 / 删除任务  -->
        <StackPanel
            Grid.Row="3"
            Margin="0,15,0,0"
            HorizontalAlignment="Center"
            Orientation="Horizontal">
            <Button
                Width="100"
                Height="32"
                Margin="0,0,15,0"
                Click="BtnCopyTask_Click"
                Content="{DynamicResource CopyTaskButton}" />
            <Button
                Width="100"
                Height="32"
                Click="BtnDeleteTask_Click"
                Content="{DynamicResource DeleteTaskButton}" />
        </StackPanel>
    </Grid>
</hc:Window>
