<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:controls="clr-namespace:MaaWpfGui.Styles.Controls">

    <!--  A Style that affects all TextBlocks  -->
    <Style
        x:Key="MaaTextBlockDefaultStyle"
        BasedOn="{StaticResource TextBlockDefault}"
        TargetType="{x:Type controls:TextBlock}" />

    <Style BasedOn="{StaticResource MaaTextBlockDefaultStyle}" TargetType="{x:Type controls:TextBlock}" />

    <!--
        原生 TextBlock 的默认样式：水平回归原生左对齐（TextBlockDefault 默认居中），
        垂直保留居中与 controls:TextBlock 一致，避免两种 TextBlock 混排时行内垂直错位
    -->
    <Style BasedOn="{StaticResource TextBlockDefault}" TargetType="{x:Type TextBlock}">
        <Setter Property="HorizontalAlignment" Value="Left" />
    </Style>

    <!--
        Hyperlink 内的 TextBlock 专用：上面的默认样式经 TextBlockDefault 带入 Foreground
        setter（PrimaryTextBrush），会压过从 Hyperlink 继承的链接色，这里显式恢复。
        色值取自 HandyControl 为 Hyperlink 设定的主题链接色，HC 调整时需同步
    -->
    <Style
        x:Key="HyperlinkTextBlockStyle"
        BasedOn="{StaticResource TextBlockDefault}"
        TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="#FF0066CC" />
        <Setter Property="HorizontalAlignment" Value="Left" />
    </Style>
</ResourceDictionary>
