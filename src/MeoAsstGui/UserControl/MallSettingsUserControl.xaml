<UserControl x:Class="MeoAsstGui.MallSettingsUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:s="https://github.com/canton7/Stylet"
             xmlns:dd="urn:gong-wpf-dragdrop"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="550">
    <StackPanel VerticalAlignment="Center" Margin="40, 0, 40, 0">
        <!--IsEnabled="{Binding CreditShoppingCheckBoxIsEnable}"-->
        <CheckBox IsChecked="{Binding CreditShoppingBlackList1}"  Block.TextAlignment="Center"
                  HorizontalAlignment="Center" Margin="0,2" VerticalAlignment="Center"
                  Content="不买碳" />
        <CheckBox IsChecked="{Binding CreditShoppingBlackList2}"  Block.TextAlignment="Center"
                  HorizontalAlignment="Center" Margin="0,2" VerticalAlignment="Center"
                  Content="不买家具" />
        <CheckBox IsChecked="{Binding CreditShoppingBlackList3}"  Block.TextAlignment="Center"
                  HorizontalAlignment="Center" Margin="0,2" VerticalAlignment="Center"
                  Content="不买加急许可" />
        <CheckBox IsChecked="{Binding CreditShoppingBlackList4}"  Block.TextAlignment="Center"
                  HorizontalAlignment="Center" Margin="0,2" VerticalAlignment="Center"
                  Content="不买招募许可" />
    </StackPanel>
</UserControl>