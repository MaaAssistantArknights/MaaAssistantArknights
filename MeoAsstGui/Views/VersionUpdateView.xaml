<Window x:Class="MeoAsstGui.VersionUpdateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:s="https://github.com/canton7/Stylet"
             mc:Ignorable="d" 
             Title="有新版本" Height="500" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel HorizontalAlignment="Center" Orientation="Vertical" Grid.Row="0">
            <TextBlock Text="{Binding UpdateTag}" Height="50" Width="373" FontSize="24" Margin="0, 20"/>
            <TextBlock Text="{Binding UpdateInfo}" Height="250" Width="400" FontSize="16" Margin="0, 20"/>
        </StackPanel>

        <StackPanel HorizontalAlignment="Center" Orientation="Horizontal" Grid.Row="1">
            <Button Command="{s:Action Download}" Content="前往下载" Margin="20" Width="120" Height="60"/>
            <Button Command="{s:Action Close}" Content="下次一定" Margin="20" Width="120" Height="60"/>
        </StackPanel>
    </Grid>
</Window>
